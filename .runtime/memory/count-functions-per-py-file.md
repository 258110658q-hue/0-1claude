---
name: count-functions-per-py-file
description: 用户要求用子Agent统计core/目录下每个.py文件的函数定义数量
type: task
---

用户要求使用子Agent统计 `core/` 目录下每个 `.py` 文件中定义的函数数量。具体任务：遍历 `core/` 下的所有 `.py` 文件，按文件统计其中 `def` 关键字的出现次数，并给出每个文件的函数计数结果。
